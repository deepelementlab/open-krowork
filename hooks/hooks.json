{
  "SessionStart": [
    {
      "hooks": [
        {
          "type": "command",
          "command": "python -c \"import os; os.makedirs(os.path.expanduser('~/.krowork/apps'), exist_ok=True)\""
        }
      ]
    }
  ]
}
